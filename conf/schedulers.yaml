defaults:
  - _self_ 

schedulers:
  noop:
    _target_     : conf.noop.Scheduler
    optimizer    : ???
    gamma        : 0.1

  step:
    _target_     : torch.optim.lr_scheduler.MultiStepLR
    optimizer    : ???
    gamma        : 0.1
    milestones   : [30, 50]
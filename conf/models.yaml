
defaults:
  - _self_ 

models:
  noop:
    _target: conf.noop.Model
    cfg: {}

  Unet_G4_DSNT:
    _target_  : models.Unet_G.NNModule
    cfg:
      unet:
        in_channels: 1
        out_channels: 4
        n_blocks: 4
        start_filters: 6
        activation: 'relu'
        normalization: 'batch'
        conv_mode: 'same'
        dim: 2
        up_mode: 'transposed'
      dsnt:
        T: 1
